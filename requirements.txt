flask   # Flask 3.1.1 is not released as of now (latest is 3.0.3)
flask-cors     # 6.0.1 does not exist; latest valid is 4.0.0
sqlalchemy==2.0.23    # ✅ valid for Python 3.12
pyarrow==14.0.1       # ✅ supported
dask==2024.2.1        # ✅ supported
gunicorn==23.0.0      # ✅ supported
